import {photoCategories} from './video-options.js';
import {advertisementSection,advertisementChange,advertisementClick,advertisementSubmit} from '/advertisement-ui.js';
import {discoverPage,discoverabilityChange,discoverabilityClick,discoverabilitySubmit} from '/discoverability-ui.js';
const categories=['Pet Care Awareness','Cattle & Dairy Health','Goat Farming Support','Poultry Care','Vaccination Reminder','Deworming Reminder','Seasonal Disease Prevention','Product Promotion','Clinic Consultation','Emergency Help Messaging','Festival & Local Campaign'];
const audiences=['Pet Owners (Dogs & Cats)','Cattle & Buffalo Dairy','Goat & Sheep Farmers','Poultry Farmers','General Animal Parents'];
const tones=['Educational & Informative','Warm & Friendly','Promotional & Engaging','Urgent Health Reminder','Festival Celebration'];
const languages=['Hindi','English','Hinglish'];
const templates=[
 ['Dog vaccination','Vaccination Reminder','Pet Owners (Dogs & Cats)','Explain why an individual vaccination plan matters. Invite owners to discuss their dog’s vaccination record with the clinic.'],
 ['Dairy animal care','Cattle & Dairy Health','Cattle & Buffalo Dairy','Create a seasonal awareness post about clean water, shade and observing changes in feeding. Invite farmers to seek veterinary advice for illness.'],
 ['Monsoon awareness','Seasonal Disease Prevention','Cattle & Buffalo Dairy','Create an awareness post about dry bedding, clean housing and early veterinary consultation during the monsoon.'],
 ['Deworming consultation','Deworming Reminder','General Animal Parents','Invite owners to discuss an individual parasite-control plan. Do not prescribe a medicine or universal dosing schedule.'],
 ['Goat farming','Goat Farming Support','Goat & Sheep Farmers','Create a helpful post about observing appetite, providing clean housing and arranging preventive veterinary consultations.'],
 ['Poultry care','Poultry Care','Poultry Farmers','Explain the value of hygiene, monitoring flock health and veterinary advice. Avoid drug or dosage instructions.'],
 ['Pet consultation','Clinic Consultation','Pet Owners (Dogs & Cats)','Invite pet owners to book a consultation for a preventive health discussion.'],
 ['Product spotlight','Product Promotion','General Animal Parents','Promote only the product details entered below. Do not invent stock, price, discounts or health benefits.'],
 ['Local festival greeting','Festival & Local Campaign','General Animal Parents','Write a warm local festival greeting with an animal-care reminder. Do not invent an offer.'],
 ['Emergency contact','Emergency Help Messaging','General Animal Parents','Encourage prompt veterinary contact for concerning signs. Ask readers to call to confirm availability; do not claim a 24-hour service.'],
];
const scenes=[['Pet care','A caring veterinarian gently examining a calm healthy dog in a clean Indian veterinary clinic. Warm natural light, slow camera movement.'],['Dairy farm','A healthy dairy cow in a well-ventilated shed in Jharkhand. A farmer offers fresh water, calm morning scene, cinematic close-up.'],['Goat care','Healthy goats in a clean shaded enclosure with a farmer, gentle documentary camera movement.'],['Clinic introduction','Welcoming veterinary clinic reception in a small Indian town, green and cream palette, friendly professional atmosphere.']];
let builder={title:'',platform:'Facebook',category:categories[0],audience:audiences[0],service:'',tone:tones[0],language:'Hindi',cta:'Call to book a consultation',content:'',headline:'',hashtags:'',imagePrompt:'',videoPrompt:''};
let video={title:'',prompt:'',ratio:'9:16',duration:'8',imageId:''},poster={title:'',prompt:'',ratio:'1:1'};
let photoFilter='All';
let chatText='', chatLanguage='Hindi', librarySearch='', libraryChannel='all';
let calendarMonth=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit'}).format(new Date());
// Intl month ordering varies; construct the canonical key explicitly.
const todayIST=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
calendarMonth=todayIST().slice(0,7);
let selectedDay='', calendarMode='month', pending=false;
const meta=d=>{try{return JSON.parse(d.meta_json||'{}');}catch{return {};}};
export function extraNames(){return {creator:'Content Creator',video:'Video Maker',images:'Poster & media',whatsapp:'WhatsApp',chat:'AI Assistant'};}
export function extraIcons(){return {manager:'◎',approvals:'✓',creator:'✎',video:'▷',images:'▧',whatsapp:'↗',chat:'✧'};}
function options(items,value,esc){return items.map(x=>`<option value="${esc(x)}" ${x===value?'selected':''}>${esc(x)}</option>`).join('');}
function input(label,key,value,esc,type='text',max=3000){return `<label for="${key}">${label}</label><input id="${key}" name="${key}" type="${type}" maxlength="${max}" value="${esc(value)}">`;}
function select(label,key,items,value,esc){return `<label for="${key}">${label}</label><select id="${key}" name="${key}">${options(items,value,esc)}</select>`;}
function textarea(label,key,value,esc,max=6000){return `<label for="${key}">${label}</label><textarea id="${key}" name="${key}" maxlength="${max}">${esc(value)}</textarea>`;}
function heading(title,subtitle){return `<div class="heading"><div><div class="eyebrow">RVH Marketing Studio</div><h1>${title}</h1><p>${subtitle}</p></div></div>`;}
function preview(b,p,esc){return `<div class="social-preview"><div class="social-header"><img src="/icon.svg" alt=""><div><strong>${esc(b.language==='Hindi'?p.hindiName:p.name)}</strong><small>${esc(b.platform)} · Preview</small></div></div><h3>${esc(b.headline||b.title||'Your headline')}</h3><div class="output">${esc(b.content||'Write a caption or generate one from your brief.')}</div><p>${esc(b.hashtags)}</p><div class="preview-contact">${esc(p.phone)}<br>${esc(p.address)}</div><span class="pill">${esc(b.cta)}</span></div>`;}
function builderPage(c,whatsapp=false){
 const {state,esc,list}=c;const b=whatsapp?{...builder,platform:'WhatsApp'}:builder;
 return heading(whatsapp?'WhatsApp text':'Write a post',whatsapp?'Prepare messages for your audience, review them and share approved text.':'Build Facebook, Instagram and local campaigns with a live preview.')+
 `<div class="split"><form id="builder-form" data-kind="${whatsapp?'whatsapp':'content'}" class="card"><h2>${whatsapp?'1. Describe your message':'1. Describe your post'}</h2>${input('Campaign title','b-title',b.title,esc,'text',160)}<div class="metrics-grid">${select('Platform','b-platform',whatsapp?['WhatsApp']:['Facebook','Instagram','WhatsApp','Video'],b.platform,esc)}${select('Language','b-language',languages,b.language,esc)}</div><details><summary>Optional: audience, tone and call to action</summary><div class="metrics-grid">${select('Category','b-category',categories,b.category,esc)}${select('Audience','b-audience',audiences,b.audience,esc)}${select('Tone','b-tone',tones,b.tone,esc)}${input('Call to action','b-cta',b.cta,esc)}</div></details>${textarea('What should the message be about?','b-service',b.service,esc,3000)}<div class="actions"><button class="primary" value="generate" ${pending||!state.aiConfigured?'disabled':''}>${pending?'Writing…':'Generate caption'}</button><details><summary>More writing options</summary><button class="secondary-option" value="variants" ${pending||!state.aiConfigured?'disabled':''}>Create alternative versions</button></details></div><hr><h2>2. Review your text</h2><p>Edit the result below, then save it for approval.</p><details><summary>Optional headline</summary>${input('Headline','b-headline',b.headline,esc)}</details>${textarea('Caption / message','b-content',b.content,esc,20000)}<details><summary>Optional hashtags</summary>${input('Hashtags','b-hashtags',b.hashtags,esc)}</details>${whatsapp?'<details><summary>Optional: personalise a message</summary><p class="note">Personalisation tokens: {{CustomerName}}, {{AnimalType}}, {{ClinicPhone}}, {{SpecialOffer}}. Replace customer-specific tokens before sending.</p><button type="button" data-token="{{CustomerName}}">Insert customer name</button></details>':''}<details><summary>Creative directions</summary>${textarea('Image prompt','b-imagePrompt',b.imagePrompt,esc,3000)}${textarea('Video prompt','b-videoPrompt',b.videoPrompt,esc,3000)}</details><div class="actions"><button value="save" class="primary">Save for review</button></div><p class="note">${whatsapp?'Messages are saved as drafts. Bulk sending requires a connected WhatsApp Business provider and opted-in recipients.':'AI output is a draft. Review your caption and clinic details before approval.'}</p></form><section class="card"><h2>Live preview</h2><div id="post-preview">${preview(b,state.profile,esc)}</div><h3>Need an idea? Choose one</h3><div class="template-grid">${templates.map((t,i)=>`<button type="button" data-template="${i}">${esc(t[0])}</button>`).join('')}</div></section></div><div class="section-title"><h2>${whatsapp?'Campaign history':'Recent posts'}</h2></div>${list(state.drafts.filter(d=>whatsapp?meta(d).platform==='WhatsApp':Boolean(meta(d).platform)).slice(0,12))}`;
}
function gallery(c,kind){const {state,esc,date,pill}=c;const rows=state.media.filter(m=>kind==='video'?m.kind==='video':['image','upload'].includes(m.kind)).filter(m=>kind==='video'||photoFilter==='All'||(m.category||'Unlabelled')===photoFilter);return `<div class="media-grid">${rows.map(m=>`<article class="card media-card"><div class="section-title"><h3>${esc(m.title)}</h3>${pill(m.status)}</div>${m.status==='completed'?(m.mime==='video/mp4'?`<video controls preload="metadata" src="/api/media/${m.id}/file" aria-label="${esc(m.title)}"></video>`:`<img class="asset-preview" src="/api/media/${m.id}/file" alt="${esc(m.title)}" loading="lazy">`):`<p>${m.status==='starting'?'Submitting generation request…':m.status==='processing'?'Video is generating. Progress is saved; you can leave this page.':'Generation did not complete.'}</p>`}${m.error?`<p class="banner">${esc(m.error)}</p>`:''}<small>${date(m.created_at)} · ${esc(m.ratio)}${m.duration?' · '+m.duration+' sec':''}</small><div class="actions">${m.status==='completed'?`<a class="button" href="/api/media/${m.id}/file?download">Download</a>${m.mime!=='video/mp4'?`<button data-animate="${m.id}">Animate photo</button>`:''}`:''}${m.status==='processing'?`<button data-check-media="${m.id}">Check progress</button>`:''}${!['starting','processing'].includes(m.status)&&state.role==='owner'?`<button data-delete-media="${m.id}">Delete</button>`:''}</div>${m.kind==='upload'?`<p><strong>${esc(m.category||'Unlabelled')}</strong> · ${esc(m.description||'Add a description to help your video agent.')}</p>${state.role==='owner'?`<details><summary>Edit photo details</summary><form data-photo-edit="${m.id}"><label>Title<input name="title" maxlength="160" required value="${esc(m.title)}"></label><label>Category<select name="category">${options(photoCategories,m.category||'Unlabelled',esc)}</select></label><label>What this photo shows<textarea name="description" maxlength="600">${esc(m.description||'')}</textarea></label><button>Save photo details</button></form></details>`:''}`:''}<details><summary>Generation prompt</summary><p class="output">${esc(m.prompt||'Uploaded image')}</p></details></article>`).join('')||'<div class="card empty">Your uploaded and generated media will appear here.</div>'}</div>`;}
function videoPage(c){const {state,esc}=c;return heading('Make a video','Follow three short steps, then review your scenes before generation.')+advertisementSection(c)+`<details ${video.imageId||video.prompt?'open':''}><summary>More tools: single Veo clip or standalone storyboard</summary><div class="split"><form id="video-form" class="card"><h2>Veo video generator</h2>${!state.mediaConfigured.video?'<div class="banner">Veo setup needed: add GEMINI_API_KEY in Render → Environment. A Google project with Veo API access and billing is required.</div>':''}${input('Video title','v-title',video.title,esc,'text',160)}${textarea('Describe the scene, camera movement and dialogue','v-prompt',video.prompt,esc)}<div class="actions">${scenes.map((s,i)=>`<button type="button" data-scene="${i}">${esc(s[0])}</button>`).join('')}</div><div class="metrics-grid">${select('Format','v-ratio',['9:16','16:9'],video.ratio,esc)}${select('Clip duration (seconds)','v-duration',['4','6','8'],video.duration,esc)}</div><label for="v-imageId">Animate an image (optional)</label><select id="v-imageId"><option value="">Text to video</option>${state.media.filter(m=>m.status==='completed'&&m.mime?.startsWith('image/')&&m.bytes<=6*1024*1024).map(m=>`<option value="${m.id}" ${video.imageId===m.id?'selected':''}>${esc(m.title)}</option>`).join('')}</select><p class="note">Upload a PNG or JPEG in Poster & media first. Output is 720p. A four-second clinic contact card is automatically appended when processing succeeds. Hindi speech and generated text require review.</p><label class="check"><input id="video-cost" type="checkbox" required> I understand this generates a paid video using my Google account.</label><button class="primary" ${pending||!state.mediaConfigured.video?'disabled':''}>Generate video</button><p class="note">Model: ${esc(state.mediaModels.video)}. No automatic retries that create a second paid video.</p></form><section class="card"><h2>Storyboard & voiceover</h2><p>Plan a 15, 30 or 60 second reel with timed scenes, on-screen text, narration and a closing call to action.</p><form id="story-form">${textarea('Video topic','story-topic','',esc,3000)}${select('Script duration','story-duration',['15','30','60'],'30',esc)}${select('Script language','story-language',languages,'Hindi',esc)}<button class="primary" ${!state.aiConfigured||pending?'disabled':''}>Write storyboard</button></form><div class="brand-contact"><strong>${esc(state.profile.hindiName)}</strong><p>${esc(state.profile.phone)}<br>${esc(state.profile.address)}</p></div></section></div></details><div class="section-title"><h2>Your video gallery</h2><button data-media-refresh>Refresh progress</button></div>${gallery(c,'video')}`;}
function imagePage(c){const {state,esc}=c;return heading('Photos & posters','Generate a visual, upload clinic photos, or create a branded poster without AI generation.')+`<div class="split"><form id="image-form" class="card"><h2>Make an AI poster</h2><p>Describe the poster, choose a format, then generate. To use real clinic photos, use the upload form.</p>${input('Image title','i-title',poster.title,esc,'text',160)}${textarea('Describe the image or poster','i-prompt',poster.prompt,esc)}${select('Format','i-ratio',['1:1','9:16','16:9'],poster.ratio,esc)}<label class="check"><input id="image-cost" type="checkbox" required> I understand image generation uses paid OpenAI API credits.</label><button class="primary" ${pending||!state.mediaConfigured.image?'disabled':''}>Generate image</button><p class="note">Model: ${esc(state.mediaModels.image)}. Review Hindi text and contact details in generated images before use.</p></form><div class="card"><h2>Add your real clinic photos</h2><p>Choose a photo, label the clinic area, then upload. It will be available in Make a video.</p><form id="upload-form">${input('Photo title','upload-title','',esc,'text',160)}${select('Clinic area','upload-category',photoCategories,'Entrance',esc)}${textarea('What this photo shows / confirmed strengths','upload-description','',esc,600)}<label for="upload-file">PNG or JPEG, up to 6 MB</label><input id="upload-file" type="file" accept="image/png,image/jpeg" required><button class="primary">Upload image</button></form><hr><h2>Quick branded poster</h2><form id="poster-form">${input('Headline','poster-headline','अपने पशु की देखभाल के लिए संपर्क करें',esc,'text',100)}${textarea('Short message','poster-message','पशु स्वास्थ्य परामर्श के लिए कॉल करें।',esc,350)}<button class="primary">Download branded PNG</button></form><p class="note">This template uses your saved clinic details. No AI generation charge.</p></div></div><div class="section-title"><h2>Clinic photo & media library</h2><span>${(state.mediaBytes/1024/1024).toFixed(1)} / 600 MB</span><button data-media-refresh>Refresh</button></div><label>Filter by clinic area<select id="photo-filter">${options(['All',...photoCategories],photoFilter,esc)}</select></label>${gallery(c,'image')}`;}
function chatPage(c){const {state,esc}=c;return heading('Ask for help','Type your question in Hindi or English. This helper gives advice and drafts.')+`<section class="card"><div class="actions">${state.role==='owner'?'<button data-clear-chat>Clear conversation</button>':''}</div><div class="chat-history">${state.chat.map(m=>`<article class="chat-message ${m.role}"><strong>${m.role==='user'?'You':'RVH Assistant'}</strong><div class="output">${esc(m.text)}</div>${m.role==='assistant'?`<button data-chat-draft="${m.id}">Save as draft</button>`:''}</article>`).join('')||'<p>Ask for a campaign idea, caption, video brief or weekly marketing plan.</p>'}</div><form id="chat-form">${select('Language','chat-language',languages,chatLanguage,esc)}${textarea('Your message','chat-text',chatText,esc,4000)}<button class="primary" ${pending||!state.aiConfigured?'disabled':''}>${pending?'Thinking…':'Send to assistant'}</button></form><p class="note">Shared workspace conversation. The assistant can draft and advise; it cannot browse, publish posts or send messages.</p></section>`;}
function dayKey(iso){return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(iso));}
function calendar(c){const {state,esc,list}=c;const [year,month]=calendarMonth.split('-').map(Number);const first=new Date(Date.UTC(year,month-1,1));const begin=new Date(first);begin.setUTCDate(1-first.getUTCDay());const cells=[];for(let i=0;i<42;i++){const d=new Date(begin);d.setUTCDate(d.getUTCDate()+i);cells.push(d.toISOString().slice(0,10));}
 const planned=state.drafts.filter(d=>d.status==='planned');let visible=cells;
 if(calendarMode==='week'){const anchor=selectedDay||todayIST();const idx=Math.max(0,cells.indexOf(anchor));visible=cells.slice(Math.floor(idx/7)*7,Math.floor(idx/7)*7+7);}
 return heading('Publishing calendar','Plan approved posts. All dates and times use India Standard Time.')+`<div class="banner">Planning does not automatically publish. Mark a post published only after you have posted it.</div><div class="card"><div class="actions"><button data-month="-1" aria-label="Previous month">←</button><label for="calendar-month">Month</label><input type="month" id="calendar-month" value="${esc(calendarMonth)}"><button data-month="1" aria-label="Next month">→</button><button data-calendar-mode="month">Month</button><button data-calendar-mode="week">Week</button><button data-calendar-today>Today</button><button data-calendar-all>All planned posts</button></div><div class="calendar-grid">${['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(x=>`<strong>${x}</strong>`).join('')}${visible.map(key=>{const items=planned.filter(d=>dayKey(d.planned_at)===key);return `<button class="calendar-cell ${key.slice(0,7)!==calendarMonth?'muted':''} ${key===selectedDay?'selected':''} ${key===todayIST()?'today':''}" data-day="${key}"><span>${Number(key.slice(8))}</span><small>${items.length?items.length+' planned':''}</small></button>`;}).join('')}</div></div><div class="section-title"><h2>${selectedDay?'Planned for '+esc(selectedDay):'All planned posts'}</h2></div>${list(planned.filter(d=>!selectedDay||dayKey(d.planned_at)===selectedDay).sort((a,b)=>a.planned_at.localeCompare(b.planned_at)))}`;
}
export function libraryPage(c){const {state,esc,list,filter,statusLabel}=c;return heading('Content library','Search your posts, campaigns, storyboards and assistant drafts.')+`<div class="actions"><button class="primary" data-page="creator">Create content</button><button data-new>Manual draft</button>${state.role==='owner'?'<a class="button" href="/api/export">Export workspace</a>':''}</div><div class="library-filters"><label>Search<input id="library-search" value="${esc(librarySearch)}" placeholder="Title, caption or topic"></label><label>Status<select id="filter">${['all','draft','pending','approved','planned','published'].map(s=>`<option value="${s}" ${s===filter?'selected':''}>${statusLabel[s]||'All statuses'}</option>`).join('')}</select></label><label>Channel<select id="library-channel">${options(['all','Facebook','Instagram','WhatsApp','Video'],libraryChannel,esc)}</select></label></div><div id="library-results">${libraryResults(c)}</div>`;}
function libraryResults(c){return c.list(c.state.drafts.filter(d=>(c.filter==='all'||d.status===c.filter)&&(libraryChannel==='all'||meta(d).platform===libraryChannel)&&`${d.title} ${d.content}`.toLowerCase().includes(librarySearch.toLowerCase())));}
export function profileSection(c){const {state,esc}=c;const p=state.profile;return `<section class="card"><h2>Clinic identity & brand rules</h2><p>These details are used by every agent and the poster template.</p><form id="profile-form"><fieldset ${state.role!=='owner'?'disabled':''}>${input('English name','p-name',p.name,esc,'text',120)}${input('Exact Hindi name','p-hindiName',p.hindiName,esc,'text',120)}${input('Clinic phone','p-phone',p.phone,esc,'text',30)}${input('Address','p-address',p.address,esc,'text',300)}${input('Booking link (HTTPS, optional)','p-bookingUrl',p.bookingUrl||'',esc,'url',1000)}${textarea('Confirmed services and products','p-services',p.services,esc,1000)}${input('Confirmed opening hours','p-hours',p.hours,esc,'text',200)}${textarea('Brand guidelines','p-guidelines',p.guidelines,esc,2000)}<button class="primary">Save clinic profile</button></fieldset></form></section><section class="card"><h2>Media connections</h2><p>Veo: ${state.mediaConfigured.video?'Configured':'Add GEMINI_API_KEY in Render'}</p><p>Model: ${esc(state.mediaModels.video)}</p><p>OpenAI images: ${state.mediaConfigured.image?'Key configured — model access is verified when generating':'Add OPENAI_API_KEY in Render'}</p><p>Image model: ${esc(state.mediaModels.image)}</p><p class="note">Optional Render variables: VEO_MODEL and OPENAI_IMAGE_MODEL. Provider billing and model access are required. Keys stay on the server.</p><h2>Team access</h2><p>Signed in as ${esc(state.role)}.</p><p>Marketing staff: ${state.rolesConfigured.staff?'Enabled':'Not configured'}<br>Content creator: ${state.rolesConfigured.creator?'Enabled':'Not configured'}</p><p class="note">The owner can configure distinct STAFF_PASSWORD and CREATOR_PASSWORD values in Render, each at least 16 characters. Only the owner can approve content and change clinic settings. Staff can plan approved content and record results.</p>${state.role==='owner'?'<a class="button" href="/api/export">Download workspace data</a>':''}</section>`;}
export function editorExtras(c,d){if(!d)return "";const {esc,state}=c;const m=meta(d);let result={};try{result=JSON.parse(d.metrics_json||'{}');}catch{}
 return `<section class="card"><h2>Creative tools</h2><p>${esc(m.platform||'General')} · ${esc(m.audience||'General audience')}</p><div class="actions"><button data-remix="${d.id}">Open in Content Creator</button><button data-media-brief="image" data-id="${d.id}">Create image</button><button data-media-brief="video" data-id="${d.id}">Create video</button><button data-download-draft="${d.id}">Download text</button></div>${d.published_url?`<p>Published link: <a href="${esc(d.published_url)}" target="_blank" rel="noopener noreferrer">Open published post</a></p>`:''}${['approved','planned','published'].includes(d.status)?`<button data-share-approved="${d.id}">Share approved text</button>`:''}${['approved','planned'].includes(d.status)&&state.role!=='creator'?`<form id="publish-form">${input('Published post link (HTTPS)','published-url','',esc,'url',1000)}<label class="check"><input id="published-confirm" type="checkbox" required>I have published this content on the platform.</label><button class="primary">Record as published</button></form>`:''}</section>${state.role!=='creator'?`<section class="card"><h2>Campaign results</h2><p class="note">Enter actual results from the platform. These are manual records, not delivery confirmations.</p><form id="results-form">${input('Reporting period','result-period',result.period||'',esc,'text',160)}<div class="metrics-grid">${['spend','clicks','leads','revenue','sent','failed','responses'].map(k=>`<label>${esc(k)}<input name="${k}" type="number" min="0" max="10000000000" step="${['spend','revenue'].includes(k)?'0.01':'1'}" value="${result[k]??0}" required></label>`).join('')}</div><button class="primary">Save actual results</button></form></section>`:''}`;}
export function campaignAnalytics(c){const rows=c.state.drafts.map(d=>{try{return {d,m:JSON.parse(d.metrics_json||'{}')}}catch{return {d,m:{}}}}).filter(x=>x.m.period);return `<section class="card"><h2>Campaign performance</h2><p class="note">Manually recorded periods can differ. These rows are separate from the aggregate totals above.</p><div class="table-wrap"><table><thead><tr><th>Campaign</th><th>Period</th><th>Spend ₹</th><th>Leads</th><th>Revenue ₹</th><th>ROAS</th></tr></thead><tbody>${rows.map(({d,m})=>`<tr><td><button data-draft="${d.id}">${c.esc(d.title)}</button></td><td>${c.esc(m.period)}</td><td>${m.spend}</td><td>${m.leads}</td><td>${m.revenue}</td><td>${m.spend>0?(m.revenue/m.spend).toFixed(2)+'×':'—'}</td></tr>`).join('')||'<tr><td colspan="6">Record actual campaign results from a draft’s review page.</td></tr>'}</tbody></table></div></section>`;}
export function renderStudio(page,c){return ({manager:()=>managerPage(c),approvals:()=>approvalPage(c),creator:()=>builderPage(c),whatsapp:()=>builderPage(c,true),video:()=>videoPage(c),images:()=>imagePage(c),chat:()=>chatPage(c),calendar:()=>calendar(c),discover:()=>discoverPage(c)})[page]?.();}
export function studioInput(e,c){const id=e.target.id;
 if(id.startsWith('b-')){builder[id.slice(2)]=e.target.value;const node=document.querySelector('#post-preview');if(node)node.innerHTML=preview({...builder,platform:c.page==='whatsapp'?'WhatsApp':builder.platform},c.state.profile,c.esc);}
 if(id.startsWith('v-'))video[id.slice(2)]=e.target.value;
 if(id.startsWith('i-'))poster[id.slice(2)]=e.target.value;
 if(id==='chat-text')chatText=e.target.value;
 if(id==='chat-language')chatLanguage=e.target.value;
 if(id==='library-search'){librarySearch=e.target.value;document.querySelector('#library-results').innerHTML=libraryResults(c);}
}
export function studioChange(e,c){
 advertisementChange(e,c);
 discoverabilityChange(e,c);
 if(e.target.id==='photo-filter'){photoFilter=e.target.value;c.render();}
 if(e.target.id==='library-channel'){libraryChannel=e.target.value;c.render();}
 if(e.target.id==='calendar-month'&&/^\d{4}-\d{2}$/.test(e.target.value)){calendarMonth=e.target.value;selectedDay='';c.render();}
}
function downloadBlob(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
async function posterDownload(profile,title,message){
 await document.fonts.ready;
 const canvas=document.createElement('canvas');canvas.width=1080;canvas.height=1350;const ctx=canvas.getContext('2d');
 ctx.fillStyle='#f5f7ef';ctx.fillRect(0,0,1080,1350);ctx.fillStyle='#123c36';ctx.fillRect(0,0,1080,270);ctx.fillRect(0,1040,1080,310);
 function wrap(text,x,y,width,font,color,lineHeight){ctx.font=font;ctx.fillStyle=color;let line='';for(const word of text.split(/\s+/)){const next=line?line+' '+word:word;if(ctx.measureText(next).width>width&&line){ctx.fillText(line,x,y);y+=lineHeight;line=word;}else line=next;}if(line)ctx.fillText(line,x,y);return y+lineHeight;}
 wrap(profile.hindiName,70,110,940,'bold 58px sans-serif','#fff',75);wrap(profile.name,70,225,940,'28px sans-serif','#c9d7c5',40);
 let y=wrap(title,75,400,930,'bold 56px sans-serif','#123c36',80);y=wrap(message,75,y+60,930,'36px sans-serif','#25534b',60);
 wrap('संपर्क करें · '+profile.phone,70,1130,940,'bold 46px sans-serif','#fff',60);wrap(profile.address,70,1220,940,'30px sans-serif','#e0ead9',45);
 const blob=await new Promise(r=>canvas.toBlob(r,'image/png'));downloadBlob(blob,'rvh-branded-poster.png');
}
export async function studioClick(el,c){
 if(await advertisementClick(el,c))return true;
 if(await discoverabilityClick(el,c))return true;
 const {state,api,refresh,render,notice,go}=c;
 if(el.dataset.template!==undefined){const t=templates[Number(el.dataset.template)];builder={...builder,title:t[0],category:t[1],audience:t[2],service:t[3]};render();return true;}
 if(el.dataset.scene!==undefined){video={...video,title:scenes[Number(el.dataset.scene)][0],prompt:scenes[Number(el.dataset.scene)][1]};render();return true;}
 if(el.dataset.token){builder.content+=el.dataset.token;render();return true;}
 if(el.hasAttribute('data-media-refresh')){await refresh();render();return true;}
 if(el.dataset.checkMedia){el.disabled=true;await api('media/'+el.dataset.checkMedia+'/check','POST',{});await refresh();render();return true;}
 if(el.dataset.deleteMedia){if(confirm('Delete this media permanently? Download a copy first if needed.')){await api('media/'+el.dataset.deleteMedia,'DELETE');await refresh();render();}return true;}
 if(el.dataset.animate){video.imageId=el.dataset.animate;go('video');return true;}
 if(el.hasAttribute('data-clear-chat')){if(confirm('Clear the shared conversation?')){await api('chat','DELETE');await refresh();render();}return true;}
 if(el.dataset.chatDraft){const m=state.chat.find(m=>m.id===el.dataset.chatDraft);const d=await api('studio/drafts','POST',{title:'Assistant draft',content:m.text,language:chatLanguage,agent:'assistant'});await refresh();go('editor',d.id);return true;}
 if(el.dataset.remix){const d=state.drafts.find(d=>d.id===el.dataset.remix);builder={...builder,...meta(d),title:d.title,language:d.language,content:d.content};go(meta(d).platform==='WhatsApp'?'whatsapp':'creator');return true;}
 if(el.dataset.mediaBrief){const d=state.drafts.find(d=>d.id===el.dataset.id),m=meta(d);if(el.dataset.mediaBrief==='image'){poster.title=d.title;poster.prompt=m.imagePrompt||d.content.slice(0,6000);go('images');}else{video.title=d.title;video.prompt=m.videoPrompt||d.content.slice(0,6000);go('video');}return true;}
 if(el.dataset.downloadDraft){const d=state.drafts.find(d=>d.id===el.dataset.downloadDraft);downloadBlob(new Blob([d.content],{type:'text/plain;charset=utf-8'}),'rvh-'+d.status+'-'+d.id+'.txt');return true;}
 if(el.dataset.shareApproved){const d=state.drafts.find(d=>d.id===el.dataset.shareApproved);if(!['approved','planned','published'].includes(d.status))throw Error('Approve this draft before sharing.');if(navigator.share){await navigator.share({title:d.title,text:d.content});}else{await navigator.clipboard.writeText(d.content);notice('Approved content copied. Paste it into your chosen platform.');}return true;}
 if(el.dataset.month){const [y,m]=calendarMonth.split('-').map(Number);calendarMonth=new Date(Date.UTC(y,m-1+Number(el.dataset.month),1)).toISOString().slice(0,7);selectedDay='';render();return true;}
 if(el.dataset.day){selectedDay=el.dataset.day;render();return true;}
 if(el.dataset.calendarMode){calendarMode=el.dataset.calendarMode;render();return true;}
 if(el.hasAttribute('data-calendar-today')){selectedDay=todayIST();calendarMonth=selectedDay.slice(0,7);render();return true;}
 if(el.hasAttribute('data-calendar-all')){selectedDay='';render();return true;}
 return false;
}
export async function studioSubmit(form,submitter,c){
 if(await advertisementSubmit(form,c))return true;
 if(await discoverabilitySubmit(form,c))return true;
 const {state,api,refresh,render,notice,go}=c;const value=id=>document.getElementById(id).value;
 if(form.dataset.photoEdit){const data=Object.fromEntries(new FormData(form));await api('media/'+form.dataset.photoEdit,'PATCH',data);await refresh();render();notice('Photo details saved.');return true;}
 if(form.id==='manager-form'){
  if(pending)return true;const f=new FormData(form),data={goal:f.get('goal'),availability:f.get('availability'),week:f.get('week'),budget:Number(f.get('budget'))};pending=true;const button=form.querySelector('button');button.disabled=true;button.textContent='Preparing your week…';
  try{await api('weekly-plan','POST',data);await refresh();go('approvals');notice('Seven drafts ready for your review.');}finally{pending=false;render();}return true;
 }
 if(form.id==='approval-form'){
  const ids=new FormData(form).getAll('draft');if(!ids.length)throw Error('Select drafts you have reviewed.');const items=ids.map(id=>{const d=state.drafts.find(x=>x.id===id);return {id,version:d.version};});await api('approval-batch','POST',{action:submitter?.value,items});await refresh();render();notice('Selected drafts updated.');return true;
 }
 if(form.id==='builder-form'){
   if(pending)return true;
   const b={...builder,platform:form.dataset.kind==='whatsapp'?'WhatsApp':builder.platform};
   const {title,content,language,...details}=b;
   if(submitter?.value==='save'){
     const draft=await api('studio/drafts','POST',{title,content,language,meta:details});await refresh();go('editor',draft.id);notice('Draft saved for review.');return true;
   }
   if(!title.trim()||!b.service.trim())throw Error('Enter a campaign title and topic first.');
   pending=true;render();
   try{
     const variants=submitter?.value==='variants';
     const request=`Campaign: ${title}\nPlatform: ${b.platform}\nCategory: ${b.category}\nAudience: ${b.audience}\nTone: ${b.tone}\nTopic and confirmed details: ${b.service}\nCall to action: ${b.cta}\n${variants?'Return clearly separated: primary caption, short version, long version, three alternatives, hashtags, image prompt, video prompt.':'Return ONLY the ready-to-review public caption, hashtags and booking contact details. No heading, alternative version, visual brief or conversational closing.'}`;
     const d=await api('runs','POST',{agent:form.dataset.kind==='whatsapp'?'whatsapp':'content',language,brief:request,meta:details});
     await api('drafts/'+d.id,'PATCH',{action:'edit',version:d.version,title,content:d.content,meta:details});await refresh();
     if(variants)go('editor',d.id);else {builder={...b,content:d.content};notice('Caption generated and saved in the library. Review your edits before approval.');}
   } finally{pending=false;render();}
   return true;
 }
 if(form.id==='video-form'||form.id==='image-form'){
   const isVideo=form.id==='video-form',v=isVideo?video:poster;
   await api('media','POST',{kind:isVideo?'video':'image',...v,duration:Number(v.duration||0),confirmCost:document.querySelector(isVideo?'#video-cost':'#image-cost').checked});await refresh();render();notice('Generation started. Your progress is saved in the gallery.');return true;
 }
 if(form.id==='upload-form'){
   const f=document.querySelector('#upload-file').files[0];if(!f||f.size>6*1024*1024)throw Error('Choose a PNG/JPEG up to 6 MB.');
   const title=value('upload-title')||f.name;
   const encoded=await new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result.split(',')[1]);reader.onerror=()=>reject(Error('Could not read this image.'));reader.readAsDataURL(f);});
   await api('media','POST',{kind:'upload',title,category:value('upload-category'),description:value('upload-description'),base64:encoded});await refresh();render();notice('Image saved.');return true;
 }
 if(form.id==='poster-form'){await posterDownload(state.profile,value('poster-headline'),value('poster-message'));notice('Poster downloaded. You can upload it to animate with Veo.');return true;}
 if(form.id==='story-form'){
   if(pending)return true;const topic=value('story-topic'),duration=value('story-duration'),language=value('story-language');if(!topic.trim())throw Error('Enter a video topic.');
   pending=true;
   try{const d=await api('runs','POST',{agent:'video',language,brief:`Write a ${duration}-second storyboard. Topic: ${topic}. Include timed scenes, a hook, narration, on-screen text and clinic contact ending. Each Veo clip can be 4, 6 or 8 seconds.`,meta:{platform:'Video'}});await refresh();go('editor',d.id);}finally{pending=false;}return true;
 }
 if(form.id==='chat-form'){
   if(pending)return true;pending=true;render();
   try{await api('chat','POST',{text:chatText,language:chatLanguage});chatText='';await refresh();}finally{pending=false;render();}return true;
 }
 if(form.id==='profile-form'){
   const data={};for(const k of ['bookingUrl','name','hindiName','phone','address','services','hours','guidelines'])data[k]=value('p-'+k);
   await api('profile','PUT',data);await refresh();render();notice('Clinic details saved for all agents.');return true;
 }
 if(form.id==='publish-form'){
   const d=state.drafts.find(d=>d.id===c.draftId);await api('drafts/'+d.id,'PATCH',{action:'publish',version:d.version,url:value('published-url'),confirmPublished:document.querySelector('#published-confirm').checked});await refresh();render();notice('Recorded as manually published.');return true;
 }
 if(form.id==='results-form'){
   const d=state.drafts.find(d=>d.id===c.draftId),metrics={period:value('result-period')};for(const k of ['spend','clicks','leads','revenue','sent','failed','responses'])metrics[k]=Number(form.elements[k].value);
   await api('drafts/'+d.id,'PATCH',{action:'results',version:d.version,metrics});await refresh();render();notice('Campaign results saved.');return true;
 }
 return false;
}

function managerPage(c){return heading('Plan my week','1. Tell us your goal → 2. Create seven drafts → 3. Review and approve.')+`<form id="manager-form" class="card"><label>Week starts<input name="week" type="date" required value="${todayIST()}"></label><label>Campaign goal<textarea name="goal" maxlength="1500" required placeholder="Promote preventive consultations for local pet owners"></textarea></label><label>Confirmed appointment availability<textarea name="availability" maxlength="1000" required placeholder="Enter confirmed slots or say availability must be confirmed by phone"></textarea></label><label>Weekly marketing budget (₹)<input name="budget" type="number" min="0" max="1000000" value="0" required></label><p>Budget guides the plan; creating a plan does not buy ads or generate paid videos. Uses one AI request. Appointments are not connected yet.</p><button class="primary" ${pending||!c.state.aiConfigured||c.state.role!=='owner'?'disabled':''}>${pending?'Preparing your week…':'Create seven daily drafts'}</button></form><div class="section-title"><h2>Weekly drafts</h2><button data-page="approvals">Open approval inbox</button></div>${c.list(c.state.drafts.filter(d=>meta(d).batch))}`;}
function approvalPage(c){const rows=c.state.drafts.filter(d=>d.status==='pending');return heading('Review & approve','1. Read the drafts → 2. Tick the ones you accept → 3. Approve selected.')+`<form id="approval-form"><div class="actions"><button name="action" value="approve" class="primary" ${c.state.role!=='owner'?'disabled':''}>Approve selected</button><button name="action" value="reject" ${c.state.role!=='owner'?'disabled':''}>Return selected to draft</button></div><p>Approval does not publish or send messages.</p>${rows.map(d=>`<article class="card"><label class="check"><input type="checkbox" name="draft" value="${d.id}"> ${c.esc(d.title)}</label><p>${c.esc(meta(d).platform||d.agent)} ${meta(d).week?' · Week '+c.esc(meta(d).week)+' · Day '+(Number(meta(d).day)+1):''}</p><div class="output">${c.esc(d.content)}</div><button type="button" data-draft="${d.id}">Edit draft</button></article>`).join('')||'<div class="card">No drafts awaiting approval.</div>'}</form>`;}
